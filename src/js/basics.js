const API_VERSION = 'v1'

// this is for development only : start.
const DEV = true

// const base_url = 'http://localhost:3000'
const base_url = 'http://192.168.1.20:3000' // my binded local server's network ip

// this is for development only : end.

export {
  API_VERSION,
  DEV,
  base_url,
}